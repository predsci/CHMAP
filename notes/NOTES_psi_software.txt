# quick commands for checking out the psi software repositories
# - doing it using svn checkout will make it a live repository (i.e. updatable/commitable)

# Check out the external dependencies repository
svn checkout --username cdowns svn+ssh://cdowns@shadow.predsci.com/opt/repositories/ext_deps/trunk ext_deps

# Check out the psi tools repository
svn checkout --username cdowns svn+ssh://cdowns@shadow.predsci.com/opt/repositories/tools/trunk psitools


